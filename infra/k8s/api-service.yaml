apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.26.0 (40646f47)
  labels:
    io.kompose.service: api
  name: api
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  selector:
    io.kompose.service: api
  type: ClusterIP
